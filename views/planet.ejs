<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            color: #333;
            padding: 20px;
        }
        h1 {
            color: #2c3e50;
            font-size: 24px;
            text-align: center;
        }
        .planet-card {
            background-color: #ecf0f1;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 10px;
            cursor: pointer;
            text-align: center;
        }
        .planet-card:hover {
            background-color: #bdc3c7;
        }
        .people-list {
            list-style-type: none;
            padding: 0;
        }
        .people-list li {
            margin: 10px 0;
        }
        .people-list li a {
            text-decoration: none;
            color: #2980b9;
        }
        .people-list li a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
<h1>People from <%= title %></h1>
<ul class="people-list">
    <% people.forEach(person => { %>
        <li><a href="/people/<%= person.name %>"><%= person.name %></a></li>
    <% }); %>
</ul>

</body>
</html>
